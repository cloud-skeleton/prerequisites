---
- ansible.builtin.set_fact:
    current_major_version: "{{ ansible_distribution_major_version | int }}"
    current_release: "{{ ansible_distribution_release }}"
    current_version: "{{ ansible_distribution_version }}"
  name: Get current Debian version
...
