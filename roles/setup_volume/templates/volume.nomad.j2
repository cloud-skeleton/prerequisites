id           = "{{ csi_volume_name }}"
name         = "{{ csi_volume_name }}"
plugin_id    = "eu.cloudskeleton.csi"
type         = "csi"

capability {
  access_mode     = "multi-node-multi-writer"
  attachment_mode = "file-system"
}
